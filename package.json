{
  "name": "deepseek-api-bridge-root",
  "version": "1.0.0",
  "description": "Root package for DeepSeek API Bridge",
  "scripts": {
    "start": "node server.js",
    "build": "npm install && cd api-server && npm install"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "node-fetch": "^2.7.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
